import Foundation

/// Represents a single day's resting heart rate measurement.
struct DailyHeartRate {
    /// The date of the measurement.
    let date: Date
    /// Resting heart rate in beats per minute.
    let bpm: Double
}
